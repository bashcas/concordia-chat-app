{
  "name": "concordia",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:web": "npm --workspace=apps/web-app run dev",
    "dev:desktop": "npm --workspace=apps/desktop-app run start",
    "build:web": "npm --workspace=apps/web-app run build"
  }
}
